name: Build LKM for KernelSU
on:
  workflow_call:
    inputs:
      expected_size:
        description: 'Exclusive manager certificate size'
        required: true
        type: string
      expected_hash:
        description: 'Exclusive manager certificate SHA-256'
        required: true
        type: string
      manager_package:
        description: 'Exclusive manager application ID'
        required: true
        type: string
  workflow_dispatch:
jobs:
  build-lkm:
    strategy:
      matrix:
        kmi:
          - android12-5.10
          - android13-5.10
          - android13-5.15
          - android14-5.15
          - android14-6.1
          - android15-6.6
          - android16-6.12
    uses: ./.github/workflows/ddk-lkm.yml
    with:
      kmi: ${{ matrix.kmi }}
      ddk_release: '20260313'
      expected_size: ${{ inputs.expected_size }}
      expected_hash: ${{ inputs.expected_hash }}
      manager_package: ${{ inputs.manager_package }}
